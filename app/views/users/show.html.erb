<div class="page">
<p><%= flash[:success] %></p>
<h1>Profile For: <%= @user.name %></h1>
<p>Email: <%= @user.email %></p>
<p>Bio: <%= @user.bio %></p>
<p>Country: <%= @user.country %></p>
<p>Preferred Language: <%= @user.language.name %></p>

<% if current_user == @user %>
<p><%= link_to "Your Contact List", contacts_path, :class => 'btn btn-primary' %></p>
<p><%= link_to 'Edit your profile', edit_user_path, :class => 'btn btn-primary' %></p>
<% end %>

<!-- @user here is the user being added to the current/logged in user's list of contacts -->
<% if current_user != @user %>
<p>  <%= form_tag(add_contact_path(@user)) do %>
    <%= submit_tag "Add contact!", :class => 'btn btn-primary'%>
    <% end %></p>
    <% end %>
</div>